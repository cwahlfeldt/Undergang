public enum UnitType
{
    Player,
    Enemy,
    Grunt,
    Sniper,
    SniperAxisQ,  // Shoots East-West (q axis: +q and -q)
    SniperAxisR,  // Shoots along r axis (+r and -r)
    SniperAxisS   // Shoots along s axis (+s and -s)
}